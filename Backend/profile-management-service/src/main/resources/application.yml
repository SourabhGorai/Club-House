# Server Configuration
server:
  port: 8082

# PostgreSQL Database Configuration
spring:
  application:
    name: profile-management-service

  datasource:
    url: jdbc:postgresql://localhost:5432/club_user
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
          jdbc:
            lob:
              non_contextual_creation: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 500KB
      max-request-size: 500KB

logging:
  level:
    root: INFO
    com:
      college:
        profile_management: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#    register-with-eureka: true
#    fetch-registry: true
#  instance:
#    prefer-ip-address: true

# In USER-SERVICE, CLUB-SERVICE2, PROFILE-MANAGEMENT-SERVICE, etc.
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}

app:
  independent-service:
    url: http://INDEPENDENT-SERVICES/api
  user-service:
    url: http://USER-SERVICE/api


###this is harsh yml file for profile-management-service
## Server Configuration
#server:
#  port: 8082
#
## PostgreSQL Database Configuration
#spring:
#  application:
#    name: profile-management-service
#
#  datasource:
#    url: jdbc:postgresql://localhost:5432/club_user
#    username: ${DB_USER}
#    password: ${DB_PASS}
#    driver-class-name: org.postgresql.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#      show-sql: true
#      properties:
#        hibernate:
#          dialect: org.hibernate.dialect.PostgreSQLDialect
#          format_sql: true
#          jdbc:
#            lob:
#              non_contextual_creation: true
#
#  servlet:
#    multipart:
#      enabled: true
#      max-file-size: 500KB
#      max-request-size: 500KB
#
#  cloud:
#    loadbalancer:
#      ribbon:
#        enabled: false
#
#logging:
#  level:
#    root: INFO
#    com:
#      college:
#        profile_management: DEBUG
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
#
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#    fetch-registry: true                # ✓ ADDED
#    register-with-eureka: true          # ✓ ADDED
#  instance:
#    prefer-ip-address: false            # ✓ CHANGED from true to false
#    hostname: localhost                 # ✓ ADDED
#    instance-id: ${spring.application.name}:${server.port}  # ✓ CHANGED
#
#app:
#  independent-service:
#    url: http://INDEPENDENT-SERVICES/api
#  user-service:
#    url: http://USER-SERVICE/api